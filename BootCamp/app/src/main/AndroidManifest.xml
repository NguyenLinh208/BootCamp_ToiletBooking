<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.toiletbooking"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="14" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@android:style/Theme.Holo.Light">

        <activity
            android:name="com.example.android.toiletbooking.activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.WaitingFormActivity"
            android:label="待ち人数"
            android:theme="@android:style/Theme.Holo.Light.Dialog" />
        <activity
            android:name=".activity.WaitingActivity"
            android:label="待ち人数"
            android:theme="@android:style/Theme.Holo.Light.Dialog" />
        <activity
            android:name="com.example.android.toiletbooking.activity.MyCounter"
            android:label="@string/finish"
            android:theme="@android:style/Theme.Holo.Light.Dialog"/>

    </application>
</manifest>
